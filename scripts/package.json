{
  "name": "@ciam-quickstart/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "tsx extract-snippets.ts",
    "aggregate": "tsx aggregate-manifests.ts",
    "validate": "tsx validate-structure.ts",
    "all": "yarn aggregate && yarn extract && yarn validate"
  },
  "dependencies": {
    "glob": "^11.0.0",
    "js-yaml": "^4.1.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
